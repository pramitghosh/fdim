## ------------------------------------------------------------------------
fdim_matrix = fdim::mb_dim(directory = system.file(package = "fdim"), shapefile = "madhya_pradesh")

## ------------------------------------------------------------------------
summary(fdim_matrix)

## ------------------------------------------------------------------------
plot(fdim_matrix)

